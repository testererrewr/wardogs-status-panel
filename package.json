{
  "name": "server-status-hub",
  "version": "3.0.0",
  "private": true,
  "description": "Multi-user Discord server status bot hosting panel for WARDOGS, FiveM, GameDig and generic JSON endpoints.",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "runner": "node src/runner.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/bot-manager.js && node --check src/db.js && node --check src/crypto.js && node --check src/server-query.js && node --check src/target-safety.js && node --check src/custom-bots.js && node --check src/runner-client.js && node --check src/runner.js && node --check src/html.js && node --check src/file-session-store.js",
    "keygen": "node -e \"console.log(require('crypto').randomBytes(32).toString('base64'))\""
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "discord.js": "^14.22.1",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "express-session": "^1.18.2",
    "gamedig": "^5.3.3",
    "helmet": "^8.1.0",
    "multer": "^2.0.2"
  }
}
